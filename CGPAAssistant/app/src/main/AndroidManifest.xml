<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mahmudulmaruf.cgpaassistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_welcome"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".GpaCalcy"
            android:label="Fast GPA Calculator" />

        <activity android:name=".MainDialog"
            android:label="Fast GPA Calculator" />

        <activity android:name=".SavedResults"
        android:label="Fast GPA Calculator" />

        <activity android:name=".TabActivity"
            android:label="Fast GPA Calculator" />

        <activity android:name=".CourseResult"
            android:label="Fast GPA Calculator" />

        <activity android:name=".UserInf"
            android:label="Fast GPA Calculator" />

        <activity android:name=".UserMain"
            android:label="Fast GPA Calculator" />

    </application>

</manifest>